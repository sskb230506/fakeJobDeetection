# Server Configuration
PORT=5000

# PostgreSQL Connection String (Prisma format)
# Replace with actual credentials: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/veriwork_db?schema=public"
